# Unit 1 — Kinematics

Kinematics is the description of motion *without* worrying about what
causes it (that's Unit 3, Newton's Laws). It's the foundation every later
unit builds on, so mastery here pays off for the rest of the semester.

## Key concepts to master

- **Position, distance, and displacement** — position is where you are;
  distance is total path length traveled (scalar); displacement is the
  straight-line change in position, start to end (vector).
- **Speed vs. velocity** — speed is distance/time (scalar); velocity is
  displacement/time (vector, has a sign/direction).
- **Average vs. instantaneous velocity** — average over an interval vs.
  the value at one instant (the slope of a position–time graph *at a point*).
- **Acceleration** — rate of change of velocity, `a = Δv/Δt`; a vector.
  Positive/negative acceleration means "in the positive/negative
  direction," **not** "speeding up/slowing down."
- **The four constant-acceleration ("kinematics") equations:**
  - `v = v₀ + at`
  - `Δx = v₀t + ½at²`
  - `v² = v₀² + 2aΔx`
  - `Δx = ½(v₀ + v)t`
- **Free fall** — constant acceleration `g ≈ 9.8 m/s²` downward, independent
  of mass (ignoring air resistance). Applies on the way up *and* down.
- **Motion graphs** — position–time (slope = velocity), velocity–time
  (slope = acceleration, area = displacement), acceleration–time (area =
  change in velocity).

## Vocabulary checklist

scalar · vector · position · distance · displacement · speed · velocity ·
average velocity · instantaneous velocity · acceleration · uniform motion ·
free fall · terminal velocity (conceptual) · sign convention

## Learning objectives

By the end of this unit, students will be able to:

1. Classify a quantity as scalar or vector and explain why the distinction matters.
2. Calculate distance, displacement, average speed, and average velocity from a description or data table.
3. Read a position–time or velocity–time graph and extract velocity, acceleration, and displacement.
4. Choose the correct kinematics equation for a problem based on which variable is unknown/not given, and solve for it with correct units.
5. Solve free-fall problems, including "thrown upward" scenarios where velocity is zero at the peak but acceleration is not.
6. Explain, in words, why acceleration and velocity can have different signs (speeding up vs. slowing down vs. reversing direction).

## Assessment criteria

| Skill | Meets expectations |
|---|---|
| Vector vs. scalar reasoning | Correctly distinguishes distance/displacement and speed/velocity in at least 4/5 scenarios |
| Equation selection | Chooses an equation consistent with the unknown/given variables without needing prompting |
| Graph reading | Extracts velocity from position–time slope and displacement from velocity–time area with correct sign |
| Free fall | Solves both "dropped" and "thrown upward" problems, correctly using `v = 0` only at the peak, not for the whole trip |
| Communication | Explains a real-world kinematics scenario using correct vocabulary (see `resources.md`) |

## NGSS alignment

Kinematics has no standalone HS performance expectation of its own —
NGSS moves straight to force/motion analysis (HS-PS2-1). This unit exists
to build the position/velocity/acceleration graph-reading and equation
fluency that HS-PS2-1 assumes students already have when they start
analyzing force data in Unit 3. See [`NGSS-ALIGNMENT.md`](../NGSS-ALIGNMENT.md).

## Files in this unit

- [`cornell-notes.md`](cornell-notes.md) — Cornell-format notes for this unit
- [`quiz.html`](quiz.html) — interactive quiz built from the most commonly missed kinematics questions
- [`resources.md`](resources.md) — external resources, practice problems, real-world applications
