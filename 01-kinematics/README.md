# Kinematics

Kinematics is the branch of physics that describes how objects move through space and time — position, distance, displacement, speed, velocity, and acceleration — without worrying about the forces that cause the motion. It gives you the mathematical language (equations and graphs) to predict where a moving object will be, how fast it will be going, and when, as long as acceleration is constant. Mastering kinematics is the foundation for everything that follows in mechanics, including forces (dynamics), projectile motion, and circular motion.

## Key Concepts to Master

- **Position**: An object's location relative to a chosen reference point (origin), expressed with a sign to indicate direction along a number line or axis.
- **Distance vs. displacement**: Distance is the total length of the path traveled (scalar, always positive, never decreases). Displacement is the straight-line change in position from start to end, including direction (vector; can be zero even if distance traveled is large, e.g., a round trip).
- **Speed vs. velocity**:
  - Average speed = total distance / total time (scalar).
  - Average velocity = displacement / time interval (vector; can be zero, positive, or negative).
  - Instantaneous velocity = the velocity at one specific moment in time — the slope of the position-time graph at that instant.
- **Acceleration**: The rate of change of velocity with respect to time, a = Δv / Δt (vector; can be positive, negative, or zero). Acceleration can exist even when velocity is momentarily zero (e.g., the peak of a vertical toss).
- **The kinematic equations for constant acceleration**. Let v₀ = initial velocity, v = final velocity, a = (constant) acceleration, t = time elapsed, and Δx (or x, or d) = displacement:
  1. v = v₀ + at
  2. Δx = v₀t + ½at²
  3. v² = v₀² + 2aΔx
  4. Δx = ½(v₀ + v)t
- **Free fall / gravitational acceleration**: Near Earth's surface, in the absence of air resistance, all objects accelerate downward at g = 9.8 m/s², regardless of their mass. This value is typically assigned a sign (e.g., a = −9.8 m/s²) depending on the chosen positive direction.
- **Interpreting position-time and velocity-time graphs**:
  - On a position-time graph, the **slope** equals velocity (steeper = faster; flat = at rest; negative slope = moving in the negative direction).
  - On a velocity-time graph, the **slope** equals acceleration, and the **area under the curve** equals displacement.
- **Sign conventions**: Before solving any problem, choose a positive direction (e.g., "up and right are positive"). Apply that choice consistently to position, velocity, and acceleration throughout the problem — a common source of error is switching conventions partway through.

## Learning Objectives

By the end of this section, students will be able to:

1. **Distinguish between** distance and displacement, and between speed and velocity, given a description or diagram of an object's motion.
2. **Calculate** average speed, average velocity, and instantaneous velocity from position-time data or graphs.
3. **Calculate** acceleration from velocity-time data and **explain** how an object can have zero velocity but nonzero acceleration at a given instant.
4. **Interpret** position-time and velocity-time graphs, correctly identifying slope as velocity or acceleration and area under a velocity-time curve as displacement.
5. **Select and apply** the correct kinematic equation to solve for an unknown quantity given three known quantities in a constant-acceleration scenario.
6. **Solve** free-fall problems using g = 9.8 m/s², including objects dropped, thrown downward, and thrown upward.
7. **Derive** one kinematic equation from another (e.g., derive Δx = ½(v₀ + v)t from v = v₀ + at and Δx = v₀t + ½at²) to demonstrate understanding of their algebraic relationships.
8. **Apply** consistent sign conventions to solve multi-step motion problems involving direction changes, and **justify** why negative acceleration does not always mean an object is slowing down.

## Assessment Criteria

| Skill / Objective | Proficient Mastery Indicator |
|---|---|
| Distance vs. displacement, speed vs. velocity | Correctly computes both quantities for a path that changes direction (e.g., round trip) and explains why they differ, without averaging speed values incorrectly. |
| Average vs. instantaneous velocity | Correctly reads instantaneous velocity as the slope of a position-time graph at a point, and average velocity as total displacement over total time, using the right one for the question asked. |
| Acceleration and its independence from velocity's sign/value | Correctly identifies situations with zero velocity but nonzero acceleration, and explains that negative acceleration only means "slowing down" when velocity is positive. |
| Graph interpretation | Reads slope and area correctly on both position-time and velocity-time graphs, and matches a description of motion to the correct graph shape. |
| Choosing/applying kinematic equations | Given any three of the five variables (v₀, v, a, t, Δx), selects the one equation that omits the unknown-irrelevant variable and solves correctly with proper units. |
| Free fall | Sets up free-fall problems with the correct sign for g, solves for time, velocity, or height, and correctly states that mass does not affect the rate of free fall. |
| Sign conventions | Maintains one consistent positive direction throughout a multi-part problem, including when velocity or displacement reverses sign. |
| Derivation of equations | Shows valid algebraic steps combining two kinematic equations to produce a third, without skipping justification. |

## Prerequisites

- Algebraic manipulation and solving multi-variable equations for one unknown
- Reading and plotting points on a Cartesian graph; understanding slope as rise/run
- Working with units (m, s, m/s, m/s²) and basic dimensional consistency
- Significant figures and appropriate rounding in numeric answers
- Basic understanding of vectors vs. scalars (helpful, reinforced within this unit)
