# Unit 1: Kinematics

[← Back to Project Outline](../../PROJECT_OUTLINE.md) · [Interactive Quiz →](quiz.html)

**Standards:** Foundational unit (CA Physics Content Standards 1a–1c); prerequisite
skill set for NGSS HS-PS2-1 (Newton's second law), taught before forces so students
have the graphing and algebra tools to analyze force data later.

**Essential question:** How do we describe and predict the motion of an object using
position, velocity, and acceleration?

## Key Concepts to Master

- **Position, distance, and displacement** — displacement is a vector (has direction);
  distance is a scalar (path length only).
- **Speed vs. velocity** — speed is a scalar (magnitude only); velocity is a vector
  (magnitude + direction).
- **Acceleration** — the rate of change of velocity; can speed an object up, slow it
  down, or change its direction. Sign (+/–) indicates direction, not "speeding up" or
  "slowing down" by itself.
- **Motion graphs** — position-time (x-t), velocity-time (v-t), and
  acceleration-time (a-t) graphs, and how to move between them:
  - slope of x-t graph = velocity
  - slope of v-t graph = acceleration
  - area under v-t graph = displacement
  - area under a-t graph = change in velocity
- **Kinematic equations** (valid only for *constant* acceleration):
  - v = v₀ + at
  - x = x₀ + v₀t + ½at²
  - v² = v₀² + 2a(x − x₀)
  - x = x₀ + ½(v₀ + v)t
- **Free fall** — a special case of constant acceleration where a = −g ≈ −9.8 m/s²
  (near Earth's surface, ignoring air resistance).

## Cornell Notes

| Cue / Question | Notes |
|---|---|
| What's the difference between distance and displacement? | Distance = total path length traveled (scalar, always ≥ 0). Displacement = straight-line change in position from start to end (vector, can be negative). Example: walking 3 m forward then 3 m back → distance = 6 m, displacement = 0 m. |
| What's the difference between speed and velocity? | Speed = distance/time (scalar). Velocity = displacement/time (vector). Average speed ≠ average velocity unless motion is one direction only. |
| What does the slope of an x-t graph tell you? | Instantaneous velocity at that point. A curved x-t graph means changing velocity (i.e., acceleration is nonzero). |
| What does the slope of a v-t graph tell you? | Acceleration. A flat (horizontal) v-t line means zero acceleration — constant velocity. |
| What does the area under a v-t graph tell you? | Displacement over that time interval (can be found by geometry: rectangles + triangles). |
| Can an object have zero velocity and nonzero acceleration at the same instant? | Yes — e.g., a ball thrown straight up has v = 0 at the top of its path, but acceleration is still −g the whole time (gravity never turns off). |
| When can I use the kinematic equations? | Only when acceleration is constant over the interval. If acceleration changes (e.g., with air resistance that depends on speed), you need calculus or numerical methods instead. |
| What is "free fall"? | Motion under gravity alone (no air resistance). Acceleration is constant at g ≈ 9.8 m/s² downward for *every* object, regardless of mass. |

**Summary:** Motion is fully described by position, velocity, and acceleration as
functions of time. Graphs and equations are two representations of the same
information — you should be able to translate between a written description, a
graph, and an equation for any motion problem. The single biggest source of errors
in this unit is mixing up scalar and vector quantities, and misreading what a
graph's slope vs. area actually represents.

## Most Commonly Missed Question Types

Try the **[interactive quiz](quiz.html)** — it's built specifically from the
question types students miss most often on this topic:
1. Distance vs. displacement in round-trip problems
2. Reading slope vs. area on a v-t graph
3. Sign of acceleration when an object is slowing down while moving in the
   negative direction (acceleration is *positive* — a common trap)
4. Velocity and acceleration at the peak of a vertical throw
5. Choosing the correct kinematic equation when time isn't given

## Learning Resources

- [PhET Interactive Simulations — Moving Man](https://phet.colorado.edu/en/simulations/moving-man) — build position/velocity/acceleration graphs by dragging a figure.
- [Khan Academy — One-dimensional motion](https://www.khanacademy.org/science/physics/one-dimensional-motion) — video lessons + practice sets on displacement, velocity, and acceleration.
- [CK-12 Physics — Motion](https://www.ck12.org/physics/) — free textbook chapter with worked examples.
- [The Physics Classroom — Kinematics](https://www.physicsclassroom.com/class/1DKin) — concept-first explanations with self-check quizzes.
- Lab: motion detector (or phone accelerometer app) + cart on a track to generate real x-t and v-t data.

## Practice Problems

1. A car accelerates from rest at 2.5 m/s² for 6 s. Find its final velocity and the
   distance it travels.
2. A ball is thrown straight up at 20 m/s. Find the time to reach maximum height,
   the maximum height, and the total time in the air (ignore air resistance).
3. Sketch the v-t graph for an object that speeds up from rest to 10 m/s in 5 s,
   travels at constant velocity for 5 s, then decelerates to rest in 2 s. Find the
   total distance traveled using the area under the graph.
4. A runner walks 40 m east, then 15 m west, in 20 s total. Find the runner's
   average speed and average velocity.
5. Two cars start 100 m apart and drive toward each other, one at 15 m/s and the
   other at 10 m/s (both constant velocity). When and where do they meet?

## Real-World Applications

- **Traffic/reaction-time analysis:** stopping distance = reaction distance
  (constant velocity) + braking distance (constant deceleration) — used in
  driver's ed and road safety design.
- **Sports analytics:** sprinters' velocity-time profiles (acceleration phase vs.
  top-speed phase) used by coaches.
- **Elevators and roller coasters:** engineers design acceleration/jerk limits for
  passenger comfort and safety.
- **Spacecraft launch profiles:** mission planners use kinematic equations to plan
  burn times and staging.

## Learning Objectives

Students will be able to:
- LO1.1 — Distinguish distance from displacement and speed from velocity in
  written, graphical, and numerical problems.
- LO1.2 — Construct and interpret x-t, v-t, and a-t graphs for an object's motion,
  including converting between graph types.
- LO1.3 — Select and apply the correct kinematic equation to solve for an unknown
  motion variable, given constant acceleration.
- LO1.4 — Analyze free-fall motion, including the sign and value of acceleration
  at every point in the motion (including at the top of a vertical throw).
- LO1.5 — Collect and analyze real motion data (lab or simulation) and compare it
  to kinematic predictions.

## Assessment Criteria

| Level | Descriptor for this unit |
|---|---|
| Exceeds | Solves multi-step problems combining graph reading and equations (e.g., find acceleration from a v-t graph, then use it to predict displacement); correctly explains vector vs. scalar reasoning in writing. |
| Meets | Correctly applies kinematic equations given known variables; correctly reads slope/area from graphs; uses correct units and sig figs. |
| Approaching | Picks a reasonable equation but makes sign or unit errors; confuses velocity and acceleration on graphs. |
| Beginning | Cannot identify which kinematic quantity (position/velocity/acceleration) a problem is asking for. |
