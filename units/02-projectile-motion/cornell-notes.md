# Cornell Notes — Unit 2: Projectile Motion

**Topic:** Describing Motion in Two Dimensions Under Gravity
**Date:** _______________
**Name:** _______________

---

| Cue Column | Notes |
|---|---|
| Why can we treat horizontal and vertical motion separately? | Gravity only acts vertically, so it never changes the horizontal velocity. The two directions are independent — solve them as two separate 1-D problems that share the same clock, t. |
| What happens to horizontal velocity during flight? | It stays constant the whole time (ignoring air resistance): aₓ = 0, so vₓ = vₓ₀ from launch to landing. Nothing about gravity or the vertical motion changes it. |
| What happens to vertical velocity during flight? | It behaves exactly like free fall from Unit 1: a = −g ≈ −9.8 m/s² (up positive), constant the entire flight, including at the very top of the arc. |
| How do I find vₓ₀ and vy₀ from launch speed and angle? | vₓ₀ = v₀ cos(θ); vy₀ = v₀ sin(θ), where θ is measured from the horizontal. |
| What determines time of flight? | Only the vertical motion — initial vertical velocity and the launch/landing height. Horizontal speed has *no effect whatsoever* on how long the object stays in the air. |
| Range formula — when does it apply? | R = v₀² sin(2θ) / g works *only* when launch height = landing height. If they differ (cliff, table, window), go back to the vertical position equation and solve for t directly — don't force the formula. |
| Maximum height formula | H = (v₀ sin θ)² / (2g) — reached at the instant vy = 0. |
| What is the velocity at the very top of the arc? | Vertical component = 0. Horizontal component = vₓ₀, unchanged. Velocity is *not* entirely zero — only its vertical piece is. |
| What shape is the trajectory? | A parabola — constant horizontal velocity plus uniformly changing vertical velocity traces that curve. |
| Complementary angles (30°/60°, 20°/70°, etc.) | Same range on level ground, because sin(2θ) = sin(180° − 2θ). They do *not* have the same time of flight or max height — the steeper angle goes higher and stays up longer. |
| What launch angle maximizes range? | 45°, on level ground, all else equal (maximizes sin(2θ) = 1). |
| Horizontally-launched projectile (off a cliff/table) | vy₀ = 0 at launch. Falls vertically exactly like a dropped object released at the same instant — same time to hit the ground — while still carrying vₓ₀ forward the whole time. |
| Common misconception #1 | "Horizontal velocity changes during flight." FALSE — with no air resistance, vₓ is constant from launch to landing. |
| Common misconception #2 | "At the peak, velocity is completely zero." FALSE — only the vertical component is zero; the object is still moving horizontally at vₓ₀. |
| Common misconception #3 | "A ball thrown horizontally off a cliff takes longer to fall than one just dropped straight down." FALSE — both hit the ground at the same time, since vertical motion doesn't care about horizontal velocity. |
| Common misconception #4 | "The range formula R = v₀²sin(2θ)/g always works." FALSE — it only holds when launch and landing heights are equal; otherwise solve from the position equations. |

---

## Summary (write in your own words after class)

_In 3–5 sentences: Why are the horizontal and vertical motions of a
projectile independent of each other? What is true about the velocity
of a projectile at the top of its trajectory, and why do people get this
wrong? Give one real-world example that uses this idea._

________________________________________________________________________
________________________________________________________________________
________________________________________________________________________
________________________________________________________________________
